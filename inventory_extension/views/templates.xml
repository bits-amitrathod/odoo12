<odoo>
    <data>
        <record id="view_picking_form_mark_all_button" model="ir.ui.view">
            <field name="name">stock.picking.form.mark.all.button</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <data>
                    <xpath expr="/form/header/button[last()]" position="after">
                        <button name="action_button_mark_all_done" invisible="context.get('active_model') == 'purchase.order'" type="object"
                                string="Mark All Done"/>
                    </xpath>
                </data>
            </field>
        </record>
    </data>
</odoo>