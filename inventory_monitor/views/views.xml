<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="inventory_monitor.list">
      <field name="name">inventory_monitor list</field>
      <field name="model">inventory_monitor.inventory_monitor</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="inventory_monitor.action_window">
      <field name="name">inventory_monitor window</field>
      <field name="res_model">inventory_monitor.inventory_monitor</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="inventory_monitor.action_server">
      <field name="name">inventory_monitor server</field>
      <field name="model_id" ref="model_inventory_monitor_inventory_monitor"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="inventory_monitor" id="inventory_monitor.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="inventory_monitor.menu_1" parent="inventory_monitor.menu_root"/>
    <menuitem name="Menu 2" id="inventory_monitor.menu_2" parent="inventory_monitor.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="inventory_monitor.menu_1_list" parent="inventory_monitor.menu_1"
              action="inventory_monitor.action_window"/>
    <menuitem name="Server to list" id="inventory_monitor" parent="inventory_monitor.menu_2"
              action="inventory_monitor.action_server"/>
    -->
  </data>
</odoo>