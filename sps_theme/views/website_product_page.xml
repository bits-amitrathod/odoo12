<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- add some info on product detials page  checked-->
    <template id="sps_product_page_inherit" inherit_id="website_sale.product" name="SPS Product Page Inherit">
        <xpath expr="//p[@t-field='product.description_sale']" position="replace">
             <!-- Manufacturer name -->
             <p class="brand-code">
                <a t-att-href="keep('/shop/brands', brand=product.product_brand_id.id)" t-field="product.product_brand_id.name" class="linkCstm"></a>
                <!-- SKU -->
                <span t-field="product.default_code"></span>
            </p>
        </xpath>
        <t t-call="website_sale.product_price" position="replace">
            <t t-if="combination_info['price'] &gt; 0">
                <t t-call="website_sale.product_price"/>
            </t>
            <t t-else="">
                <div class="mt16 mb16">
                    <p class="call-for">Call for Pricing 412-564-1280</p>
                </div>
            </t>
        </t>

        <xpath expr="//div[@id='o_wsale_cta_wrapper']" position="replace">
            <hr class="w-100"></hr>
            <div id="o_wsale_cta_wrapper" class="d-flex flex-wrap align-items-center">
                <t t-set="hasQuantities" t-value="false"/>
                <t t-set="hasBuyNow" t-value="false"/>
                <t t-set="ctaSizeBig" t-value="not hasQuantities or not hasBuyNow"/>

                <div t-if="product.actual_quantity &gt; 0" class="w-100">
                    <div t-attf-class="availability qty-name availability_message_#{product_template}">
                        <p class="availability_shop"><strong>Quantity : </strong> <t t-esc="product.actual_quantity"/> item(s) left</p>
                    </div>
                    <div t-attf-class="css_quantity input-group {{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-inline-flex'}} me-2 mb-2 align-middle #{'input-group-lg' if ctaSizeBig else ''}" contenteditable="false">
                        <a t-attf-href="#" class="btn btn-link js_add_cart_json" aria-label="Remove one" title="Remove one">
                            <i class="fa fa-minus"></i>
                        </a>
                        <input type="text" class="form-control quantity text-center" data-min="1" name="add_qty" t-att-value="add_qty or 1"/>
                        <a t-attf-href="#" class="btn btn-link float_left js_add_cart_json" aria-label="Add one" title="Add one">
                            <i class="fa fa-plus"></i>
                        </a>
                    </div>

                    <div class="w-100">
                        <t t-call="website_sales.category_vender"/>
                        <t t-call="website_sales.productExpirationDate"/>
                    </div>

                    <div id="add_to_cart_wrap" t-attf-class="{{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-block'}}  mb-2 me-auto w-100">
                        <a data-animation-selector=".o_wsale_product_images" role="button" id="add_to_cart" t-attf-class="btn btn-info mt8 js_check_product a-submit flex-grow-1 btn-lg" href="#">
<!--                            <i class="fa fa-shopping-cart me-2"/>-->
                            <span>Add to Cart</span>
                        </a>
                        <div id="product_option_block" class="d-flex flex-wrap w-100"/>
                    </div>
                </div>

                <div t-else="" t-attf-class="availability_message_#{product_template} linkCstm mt16">
                    <div style="color: #666;padding-bottom: 5px;">Get notification as soon as the product is available again. </div>
                    <div class="input-group notifcationGroup" style="width: 90%;padding-bottom: 10px;">
                        <input type="text" class="form-control" aria-label="..." t-att-value="userEmail" name="notificationEmail" t-att-data-productid="product.id"/>
                        <div class="input-group-btn">
                            <button type="button" class="btn btn-danger " aria-haspopup="true" id="notifyMe" aria-expanded="false">Notify me </button>
                        </div>
                    </div><!-- /input-group -->
                    <p style="display: none;" class="invalidEmail">
                        <i class="fa fa-exclamation-triangle"></i>
                        Email address is invalid
                    </p>
                    <div class="alert alert-success notifcationAlert" style="display: none;">You
                        will be notified when this is available.
                    </div>

                    <p style="font-size: 16px;">
                        <i class="fa fa-exclamation-triangle"/>
                        Temporarily out of stock
                    </p>
                </div>
            </div>
        </xpath>
        <t t-set="image_cols" position="replace">
            <t t-set="image_cols" t-value="(4,8)"/>
        </t>
    </template>
</odoo>