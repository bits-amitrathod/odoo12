<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Tree view -->
        <record model="ir.ui.view" id="revenue_by_bd_list_view">
              <field name="name">revenue.by.bd.view.list</field>
              <field name="model">report.bd.revenue</field>
              <field name="priority">20</field>
              <field name="arch" type="xml">
                <tree create="false" edit="false" delete="false">
                <field name="currency_id" invisible="1"/>
                <field name="sale_order_id"/>
                <field name="customer"/>
                <field name="business_development"/>
                <field name="delivery_date" widget="date"/>
                <field name="state"/>
                <field name="total_amount"  widget="monetary" options="{'currency_field': 'currency_id'}"/>
                </tree>
              </field>
        </record>

        <!-- Tree view ends here -->

        <!-- Form view-->
        <record id="revenue_by_bd_form_view" model="ir.ui.view">
          <field name="name">report.revenue.by.bd.form</field>
          <field name="model">report.bd.revenue</field>
          <field name="priority">20</field>
          <field name="arch" type="xml">
              <form create="false" edit="false" delete="false">
                  <sheet>
                      <group>
                            <group>
                                <field name="sale_order_id"/>
                                <field name="customer"/>
                                <field name="business_development"/>
                                <field name="delivery_date" widget="date"/>
                            </group>
                            <group>
                                <field name="currency_id" invisible="1"/>
                                <field name="delivery_date" widget="date"/>
                                <field name="total_amount"  widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="state"/>
                            </group>
                      </group>
                  </sheet>
              </form>
          </field>
        </record>

        <!-- Form view ends here -->


        <!-- Popup code -->
        <record id="bd_revenue_popup_view" model="ir.ui.view">
            <field name="name">Revenue By Business Development</field>
            <field name="model">popup.bd.revenue</field>
            <field name="arch" type="xml">
                <form string="Choose your date">
                    <group>
                        <group>
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                        <group>
                            <field name="business_development"/>
                        </group>
                    </group>
                    <footer>
                        <button name="open_table" string="View Report" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-default" special="cancel" />
                    </footer>
                </form>
            </field>
        </record>

        <!-- Popup code ends here-->


        <!-- Action code -->
        <record id="action_revenue_by_bd" model="ir.actions.act_window">
            <field name="name">Revenue By Business Development</field>
            <field name="res_model">popup.bd.revenue</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="bd_revenue_popup_view"/>
            <field name="target">new</field>
            <field name="context">{'valuation': True}</field>
        </record>

        <!-- Action code ends here -->

        <!-- Export popup code -->
        <record id="export_revenue_by_bd_popup" model="ir.ui.view">
            <field name="name">report.revenue.by.bd.export.popup</field>
            <field name="model">report.bd.revenue.export</field>
            <field name="arch" type="xml">
                <form string="Export Revenue By Business Development Report">
                    <group>
                        <group>
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                        <group>
                            <field name="business_development"/>
                        </group>
                        <group>
                            <div>Click On ' Export To File ' button to export Revenue By Business Development report file</div>
                        </group>
                    </group>
                    <footer>
                        <button name="download_excel_bd_revenue" string="Export To File" type="object"
                                class="btn-primary"/>
                        <button string="Cancel" class="btn-default" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Export popup code ends here -->

        <!-- Filter's code -->
        <record id="revenue_by_bd_filter" model="ir.ui.view">
            <field name="name">revenue.by.bd.filter</field>
            <field name="model">report.bd.revenue</field>
            <field name="arch" type="xml">
                <search string="Search">
                    <field name="customer"/>
                    <field name="sale_order_id"/>
                    <field name="business_development"/>
                    <group expand="0" string="Group By">
                        <filter name="customer" string="Customer Name" domain="[]" context="{'group_by':'customer'}"/>
                        <filter name="sale_order_id" string="Sale Order#" domain="[]" context="{'group_by':'sale_order_id'}"/>
                        <filter name="business_development" string="Business Development" domain="[]" context="{'group_by':'business_development'}"/>
                        <filter name="delivery_date" string="Delivery Date" domain="[]" context="{'group_by':'delivery_date'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Filter's code ends here -->

        <!-- Export Action code -->
        <act_window name="Export Revenue By Business Development Report"
                    res_model="report.bd.revenue.export"
                    src_model="report.bd.revenue"
                    view_mode="form"
                    target="new"
                    key2="client_action_multi"
                    id="revenue_by_bd_export_act_window"/>

        <!-- Export Action code ends here -->

        <!-- Menus declaration -->
        <menuitem id="sale.revenue_by_bd_menu"
                  name="Revenue By BD"
                  action="action_revenue_by_bd"
                  parent="account_closed_by_bd.business_development_reports"
                  sequence="2"/>

         <!--sequence="2"/>-->

         <!--parent="account_closed_by_bd.business_development_reports"-->

                parent="sale.menu_sale_report"

        <!-- Menus declaration ends here -->

    </data>
</odoo>