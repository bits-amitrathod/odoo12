<odoo>
    <!-- This email template Used for send email after Pick Operation is done    -->
    <record id="pick_done_ka_and_cs_email_template" model="mail.template">
        <field name="name">Pick Notification: Pick Done Email</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="email_from">{{ctx.get('email_from')}}</field>
        <field name="email_to">{{ctx.get('email_to')}}</field>
        <field name="subject">{{ctx.get('subject')}}</field>
        <field name="body_html" type="html">
                <div>Hi <t t-esc="ctx['facility_name']"/>, Pick Done For <t t-esc="ctx['so_name']"/>,
                <br/><br/>
                    <center>
                        <a t-att-href="ctx['access_url']"
                           style="background-color: #1abc9c; padding: 20px; text-decoration: none; color: #fff; border-radius: 5px; font-size: 16px;"
                           class="o_default_snippet_text">
                            View Sale Order
                        </a>
                        <br/><br/><br/>
                    </center>
                 <br/>
                </div>
        </field>
    </record>
</odoo>